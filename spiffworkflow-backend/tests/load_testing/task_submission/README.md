# How to use

1. Create a process model called "parallel-task"
1. Copy the parallel-task.bpmn file into it and start / restart spiff api server
1. Run `SPIFF_API_KEY="..." NUM_TASKS=10 ./tests/load_testing/task_submission/run_parallel_tasks_test.sh`
