[project]
name = "civitos-browser-tests"
version = "0.1.0"
description = "Playwright browser tests"
requires-python = ">=3.10"
dependencies = [
    "axe-playwright-python>=0.1.8",
    "pytest-playwright",
]

[tool.pytest.ini_options]
# Specify the test paths relative to this pyproject.toml
testpaths = ["."]
# You can add other pytest options here, for example:
# addopts = "--headed --browser=chromium"
